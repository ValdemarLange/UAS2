When interacting with tablets, the similarity transform is often used.
I think that there are two reasons for this, 
1) the transform is more descriptive than a translation and 
2) the transform can be defined using only two point pairs (similar to two fingers).

This makes the similarity transform suitable for panning and zooming on
maps.

