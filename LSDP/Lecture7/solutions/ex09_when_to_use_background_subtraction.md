# 8 When to use background subtraction

For background subtraction to work, the background should 
not move, this either means that 

1. the camera pose should be fixed relative to the background or
2. the background should be uniform and the camera should not change its
orientation so the background would change appearence.

I think that case 1 is the most often occuring situation.

